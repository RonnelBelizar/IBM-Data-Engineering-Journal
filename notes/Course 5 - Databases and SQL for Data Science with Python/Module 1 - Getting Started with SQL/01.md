# Topic: Module 1 - Getting Started With  SQL

---

# 🧠 Introduction to Databases — Made Simple & Fun!

Let’s break down the world of databases and SQL in a way that actually makes sense (and doesn’t make your brain melt 🍳).

---

## 💾 What is Data?
Data is just **information** — raw facts or figures that can be stored and processed.  
Think: names, ages, test results, or even the songs in your playlist.  
When data is organized, it becomes **useful** — like turning random ingredients into a full meal. 🍱

---

## 🗃️ What is a Database?
A **database** is where all that data lives — a structured collection of information that you can store, search, and update anytime.  
Imagine it as a super-organized digital filing cabinet where everything has its place.  
Instead of flipping through papers, you use queries to get what you want in seconds. ⚡

---

## 🧩 What is RDBMS?
RDBMS stands for **Relational Database Management System**.  
It’s just a fancy name for a system that stores data in **tables** (rows & columns) — kind of like an Excel sheet but way smarter.  
Each table relates to others through **keys**, making your data organized and connected.

Popular examples: **MySQL**, **PostgreSQL**, **SQLite**, **Oracle**, and **SQL Server**.

---

## 💬 What is SQL?
**SQL (Structured Query Language)** is how you *talk* to databases.  
You use it to tell the database what to do — kind of like giving it commands.

For example:
- "Hey DB, create a table!"
- "Insert this data!"
- "Show me all patients older than 30!"
- "Update this record!"
- "Delete that mistake!"

It’s the language of data nerds everywhere (and soon, you 👀).

---

## 🛠️ Basic SQL Commands

Here are the core commands, written in string form so you can copy-paste or just admire them:

### 1. Create — make a new table
`"CREATE TABLE patients (id INT, name VARCHAR(50), age INT, gender VARCHAR(10));"`

### 2. Insert — add data
`"INSERT INTO patients (id, name, age, gender) VALUES (1, 'John Doe', 32, 'Male');"`

### 3. Select — get data
`"SELECT * FROM patients;"`  
(`*` means “everything,” but you can also pick specific columns like `"SELECT name, age FROM patients;"`)

### 4. Update — change existing data
`"UPDATE patients SET age = 33 WHERE id = 1;"`

### 5. Delete — remove data
`"DELETE FROM patients WHERE id = 1;"`

---

## 🎯 Quick Recap
- **Data** = info  
- **Database** = place to store info  
- **RDBMS** = database that uses tables  
- **SQL** = how you talk to that database  
- **Commands** = your toolbox to build, insert, view, edit, and clean up your data

---

✨ In short: SQL lets you be the boss of data. Once you learn to speak it, you can control entire databases like a wizard casting spells. 🧙‍♂️💻


---

# 🕵️‍♂️ SQL SELECT Statement — Your Data Detective Tool!

The **SELECT statement** is like your magnifying glass 🔍 — it helps you look into your database and pull out *exactly* what you need.

---

## 🎯 What is SELECT?
The `SELECT` command is used to **fetch data** from a database.  
It’s like saying, “Hey database, show me the stuff I’m looking for.”

### Basic Syntax:
`"SELECT column_name FROM table_name;"`

Example:  
`"SELECT name, age FROM patients;"`

This means:  
👉 “Show me the **name** and **age** columns from the **patients** table.”

If you want *everything*, you can use `*`:  
`"SELECT * FROM patients;"`  
(That means: show me all columns, I wanna see it all! 👀)

---

## 🧠 Adding the WHERE Clause
The `WHERE` clause is your **filter** — it helps you pick only the rows that meet certain conditions.

### Syntax:
`"SELECT column_name FROM table_name WHERE condition;"`

Example:  
`"SELECT name, age FROM patients WHERE age > 30;"`  
Translation:  
“Show me only the patients who are older than 30.”

---

## 🧩 More WHERE Examples

### Equal to  
`"SELECT * FROM patients WHERE gender = 'Female';"`  
→ Shows all female patients.

### Not equal to  
`"SELECT * FROM patients WHERE gender <> 'Male';"`  
→ Shows everyone *not* male.

### Greater than / Less than  
`"SELECT * FROM patients WHERE age >= 50;"`  
→ Patients aged 50 and up.

### Combining Conditions  
Use **AND** and **OR** to make your filters smarter.

`"SELECT * FROM patients WHERE gender = 'Male' AND age < 40;"`  
→ Male patients under 40.

`"SELECT * FROM patients WHERE gender = 'Female' OR age < 20;"`  
→ Female patients *or* anyone younger than 20.

---

## ⚡ Example Table

| id | name       | age | gender |
|----|-------------|-----|--------|
| 1  | John Doe    | 32  | Male   |
| 2  | Jane Smith  | 28  | Female |
| 3  | Alex Cruz   | 45  | Male   |
| 4  | Mia Santos  | 19  | Female |
| 5  | Luis Torres | 53  | Male   |

Example Query:  
`"SELECT name, age FROM patients WHERE age < 40;"`

✅ Result:

| name       | age |
|-------------|-----|
| John Doe    | 32  |
| Jane Smith  | 28  |
| Mia Santos  | 19  |

---

## ⚡ Quick Recap

| Command | What It Does                  | Example                              |
|----------|-------------------------------|---------------------------------------|
| `SELECT` | Choose columns to display     | `"SELECT name, age FROM patients;"`   |
| `FROM`   | Choose which table to get data from | `"FROM patients"`              |
| `WHERE`  | Filter rows based on conditions | `"WHERE age > 25"`                 |

---

💬 **In short:**  
Use `SELECT` to *see* your data, and `WHERE` to *filter* it.  
You’re basically telling your database:  
> “Show me what I want, and only what I want.” 🎯

---

# 🔢 SQL COUNT, DISTINCT, and LIMIT — The Power Trio of SELECT!

Now that you know how to `SELECT` and filter data, let’s make it *smarter*.  
Meet three super useful SQL tricks: **COUNT**, **DISTINCT**, and **LIMIT**.  
They’ll help you count stuff, find unique values, and control how much data you see. 😎

---

## 🔢 COUNT — Counting Like a Pro
`COUNT` lets you **count the number of rows** that match your query.  
Perfect when you just want *numbers*, not all the details.

### Syntax:
`"SELECT COUNT(column_name) FROM table_name;"`

### Example:
`"SELECT COUNT(*) FROM patients;"`  
→ Counts **all rows** in the patients table.

`"SELECT COUNT(gender) FROM patients WHERE gender = 'Female';"`  
→ Counts how many female patients there are.

### Example Table:

| id | name       | age | gender |
|----|-------------|-----|--------|
| 1  | John Doe    | 32  | Male   |
| 2  | Jane Smith  | 28  | Female |
| 3  | Alex Cruz   | 45  | Male   |
| 4  | Mia Santos  | 19  | Female |
| 5  | Luis Torres | 53  | Male   |

✅ Result for `"SELECT COUNT(*) FROM patients;"`:

| count |
|--------|
| 5      |

✅ Result for `"SELECT COUNT(*) FROM patients WHERE gender = 'Male';"`:

| count |
|--------|
| 3      |

---

## 🎭 DISTINCT — Get Only Unique Values
`DISTINCT` removes duplicates from your results.  
Think of it as the “no repeats” filter. 🧹

### Syntax:
`"SELECT DISTINCT column_name FROM table_name;"`

### Example:
`"SELECT DISTINCT gender FROM patients;"`

✅ Result:

| gender |
|---------|
| Male    |
| Female  |

So instead of showing “Male” three times, it shows each unique gender once. 💪

---

## 🚦 LIMIT — Control How Much You See
`LIMIT` tells the database to show *only a certain number* of rows.  
Super useful when your table is huge and you just want a quick peek.

### Syntax:
`"SELECT * FROM table_name LIMIT number;"`

### Example:
`"SELECT * FROM patients LIMIT 3;"`

✅ Result:

| id | name       | age | gender |
|----|-------------|-----|--------|
| 1  | John Doe    | 32  | Male   |
| 2  | Jane Smith  | 28  | Female |
| 3  | Alex Cruz   | 45  | Male   |

Boom — only 3 rows shown! ⚡

---

## ⚡ Quick Recap

| Command     | What It Does                         | Example                                          | Result Summary |
|--------------|--------------------------------------|--------------------------------------------------|----------------|
| `COUNT`      | Counts number of rows                | `"SELECT COUNT(*) FROM patients;"`               | Returns a single number |
| `DISTINCT`   | Shows only unique values             | `"SELECT DISTINCT gender FROM patients;"`        | Removes duplicates |
| `LIMIT`      | Restricts number of rows returned    | `"SELECT * FROM patients LIMIT 5;"`              | Shows first 5 rows only |

---

💬 **In short:**  
- 🧮 **COUNT** → "How many?"  
- 🧹 **DISTINCT** → "Show me only the unique ones."  
- ✂️ **LIMIT** → "Don’t overload me, just a few rows please."  

Together, they make your SQL queries faster, cleaner, and smarter. ⚡💻

---

# ✏️ SQL INSERT, UPDATE, and DELETE — The Edit Squad!

Now that you know how to **view** data with `SELECT`, it’s time to **change** it!  
Meet the three main commands that let you add, modify, and remove data like a pro. ⚙️

---

## 🧩 INSERT — Add New Data
The `INSERT` command is how you add a new row (record) into your table.  
Think of it like adding a new entry in your contacts list. 📱

### Syntax:
`"INSERT INTO table_name (column1, column2, column3) VALUES (value1, value2, value3);"`

### Example:
`"INSERT INTO patients (id, name, age, gender) VALUES (6, 'Ella Reyes', 27, 'Female');"`

After running that, your table now includes a new patient! 🎉

### Example Table (after INSERT):

| id | name       | age | gender |
|----|-------------|-----|--------|
| 1  | John Doe    | 32  | Male   |
| 2  | Jane Smith  | 28  | Female |
| 3  | Alex Cruz   | 45  | Male   |
| 4  | Mia Santos  | 19  | Female |
| 5  | Luis Torres | 53  | Male   |
| 6  | Ella Reyes  | 27  | Female |

---

## 🧠 UPDATE — Modify Existing Data
The `UPDATE` command changes data that’s already in your table.  
It’s like fixing a typo or updating someone’s info. 📝

### Syntax:
`"UPDATE table_name SET column_name = new_value WHERE condition;"`

### Example:
`"UPDATE patients SET age = 33 WHERE name = 'John Doe';"`

✅ This updates John’s age to 33.

### Example Table (after UPDATE):

| id | name       | age | gender |
|----|-------------|-----|--------|
| 1  | John Doe    | 33  | Male   |
| 2  | Jane Smith  | 28  | Female |
| 3  | Alex Cruz   | 45  | Male   |
| 4  | Mia Santos  | 19  | Female |
| 5  | Luis Torres | 53  | Male   |
| 6  | Ella Reyes  | 27  | Female |

🧩 **Pro Tip:**  
Always include a `WHERE` condition, or you’ll update *every single row* 😱  
Example of what *not* to do:  
`"UPDATE patients SET age = 99;"` → Boom, everyone’s 99 years old now. 👵👴

---

## 🗑️ DELETE — Remove Data
The `DELETE` command removes rows from your table.  
Use it when data is no longer needed (or it’s a test record you want gone). 🧹

### Syntax:
`"DELETE FROM table_name WHERE condition;"`

### Example:
`"DELETE FROM patients WHERE id = 6;"`

✅ This deletes Ella’s record from the table.

### Example Table (after DELETE):

| id | name       | age | gender |
|----|-------------|-----|--------|
| 1  | John Doe    | 33  | Male   |
| 2  | Jane Smith  | 28  | Female |
| 3  | Alex Cruz   | 45  | Male   |
| 4  | Mia Santos  | 19  | Female |
| 5  | Luis Torres | 53  | Male   |

🧩 **Pro Tip:**  
Just like `UPDATE`, never forget the `WHERE` clause.  
`"DELETE FROM patients;"` → deletes *everything*. That’s a bad day. 💀

---

## ⚡ Quick Recap

| Command | What It Does           | Example                                                           | Effect |
|----------|------------------------|-------------------------------------------------------------------|---------|
| `INSERT` | Adds new data          | `"INSERT INTO patients VALUES (6, 'Ella Reyes', 27, 'Female');"`  | Adds a new record |
| `UPDATE` | Changes existing data  | `"UPDATE patients SET age = 33 WHERE name = 'John Doe';"`         | Modifies data |
| `DELETE` | Removes data           | `"DELETE FROM patients WHERE id = 6;"`                            | Deletes data |

---

💬 **In short:**  
- ✨ `INSERT` → Add new stuff  
- ✏️ `UPDATE` → Fix or change stuff  
- 🗑️ `DELETE` → Remove unwanted stuff  

These three make you the ultimate **database editor** — just remember to handle them carefully! ⚡💾
